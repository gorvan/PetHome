namespace PetHome.Application.Volunteers.Shared
{
    public record AddressDto(
            string City,
            string Street,
            string HouseNumber,
            string AppartmentNumber);
}
